{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:deploy": "vitepress build && gh-pages -d .vitepress/dist -b gh-pages -m 'gh-pages: update 🚀'"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.12",
    "vue": "^3.5.21"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
